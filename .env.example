# .env.example - do NOT commit real secrets. Use Coolify UI to set env vars.

DB_HOST=your-db-host
DB_PORT=3306
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=your_db_name

JWT_SECRET=supersecret_jwt_key
PORT=3000

# Optionally let the app run initial migrations on start (true/false)
INIT_DB=true
